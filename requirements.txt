# SmartCSV – Dependencies (Optimized for Vercel <250MB limit)
flask==3.1.0
flask-cors==5.0.1
pandas==2.2.3
numpy==2.2.2
chardet==5.2.0
# Removed: scipy, scikit-learn (too large)
# Removed: gunicorn (not needed for Vercel serverless, saves ~5MB)
